import { createTheme } from '@vanilla-extract/css'

export const [themeClass, theme] = createTheme({
  colour: {
    black: '#0C183D',
    black10: '#1A2D83',
    blue: '#1835C6',
    white: '#E2F3FF'
  }
})
